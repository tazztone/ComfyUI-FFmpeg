[pytest]
pythonpath = 
testpaths = .
markers =
    unit: Fast unit tests (no ComfyUI server)
    integration: Tests requiring FFmpeg and calling node classes
    slow: Long-running video processing tests
addopts = -v --tb=short
